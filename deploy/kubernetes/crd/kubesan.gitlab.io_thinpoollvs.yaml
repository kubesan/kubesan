# SPDX-License-Identifier: Apache-2.0

# Code generated by controller-gen. DO NOT EDIT.

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: thinpoollvs.kubesan.gitlab.io
spec:
  group: kubesan.gitlab.io
  names:
    categories:
    - kubesan
    kind: ThinPoolLv
    listKind: ThinPoolLvList
    plural: thinpoollvs
    shortNames:
    - tp
    - tps
    - pool
    - pools
    singular: thinpoollv
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: VG owning the thin pool
      jsonPath: .spec.vgName
      name: VG
      type: string
    - description: '''Time since resource was created'''
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: '''Time since pool last changed activation status'''
      jsonPath: .status.conditions[?(@.type=="Active")].lastTransitionTime
      name: Activity
      type: date
    - description: '''Node where thin pool is currently active'''
      jsonPath: .status.activeOnNode
      name: Node
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              activeOnNode:
                description: |-
                  Name of node where activation is needed, or empty.
                  When changing, may only toggle between "" and non-empty.
                maxLength: 253
                pattern: ^[a-z0-9][-.a-z0-9]*$
                type: string
                x-kubernetes-validations:
                - rule: (oldSelf==self)||((oldSelf=="")!=(self==""))
              sizeBytes:
                description: Initial size of the thin pool.  Must be a multiple of
                  512.
                format: int64
                minimum: 512
                multipleOf: 512
                type: integer
                x-kubernetes-validations:
                - rule: oldSelf==self
              thinLvs:
                description: May be updated at will.
                items:
                  properties:
                    contents:
                      description: Should be set from creation and never updated.
                      properties:
                        contentsType:
                          enum:
                          - Empty
                          - Snapshot
                          type: string
                        snapshot:
                          properties:
                            sourceThinLvName:
                              description: The name of the source snapshot.
                              maxLength: 63
                              minLength: 1
                              pattern: ^[a-z][-a-z0-9]*$
                              type: string
                          required:
                          - sourceThinLvName
                          type: object
                      required:
                      - contentsType
                      type: object
                      x-kubernetes-validations:
                      - rule: oldSelf==self
                    name:
                      description: Should be set from creation and never updated.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9][-a-z0-9]*$
                      type: string
                      x-kubernetes-validations:
                      - rule: oldSelf==self
                    readOnly:
                      description: Should be set from creation and never updated.
                      type: boolean
                      x-kubernetes-validations:
                      - rule: oldSelf==self
                    sizeBytes:
                      description: |-
                        Should be 0 if readOnly is true or if marking for deletion, otherwise, it is positive and a multiple of 512.
                        May be updated at will, but the LVM thin LV's actual size will only ever increase.
                      format: int64
                      minimum: 0
                      multipleOf: 512
                      type: integer
                    state:
                      description: May be updated at will.
                      properties:
                        name:
                          enum:
                          - Inactive
                          - Active
                          - Removed
                          type: string
                      required:
                      - name
                      type: object
                  required:
                  - contents
                  - name
                  - readOnly
                  - sizeBytes
                  - state
                  type: object
                maxItems: 65536
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              vgName:
                description: Should be set from creation and never updated.
                maxLength: 63
                minLength: 1
                pattern: ^[a-zA-Z0-9_][-a-zA-Z0-9+_.]*$
                type: string
                x-kubernetes-validations:
                - rule: oldSelf==self
            required:
            - sizeBytes
            - vgName
            type: object
          status:
            properties:
              activeOnNode:
                description: The name of the node where the LVM thin pool LV is active,
                  along with any active LVM thin LVs; or "".
                maxLength: 253
                pattern: ^[a-z0-9][-.a-z0-9]*$
                type: string
              conditions:
                description: |-
                  Conditions
                  Available: The LVM volume has been created
                  Active: The last time Status.ActiveOnNode changed
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  The generation of the spec used to produce this status.  Useful
                  as a witness when waiting for status to change.
                format: int64
                type: integer
                x-kubernetes-validations:
                - rule: self>=oldSelf
              thinLvs:
                description: The status of each LVM thin LV that currently exists
                  in the LVM thin pool LV.
                items:
                  properties:
                    name:
                      description: The name of the LVM thin LV.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z][-a-z0-9]*$
                      type: string
                    sizeBytes:
                      description: The current size of the LVM thin LV.
                      format: int64
                      type: integer
                    state:
                      description: The state of the LVM thin LV.
                      properties:
                        active:
                          properties:
                            path:
                              description: The path at which the LVM thin LV is available
                                on the node where the LVM thin pool LV is active.
                              maxLength: 128
                              minLength: 1
                              pattern: ^/dev/[-_+/a-z0-9]+$
                              type: string
                          required:
                          - path
                          type: object
                        name:
                          enum:
                          - Inactive
                          - Active
                          - Removed
                          type: string
                      required:
                      - name
                      type: object
                  required:
                  - name
                  - sizeBytes
                  - state
                  type: object
                maxItems: 65536
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            required:
            - observedGeneration
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
