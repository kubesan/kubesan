# SPDX-License-Identifier: Apache-2.0

# Code generated by controller-gen. DO NOT EDIT.

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: volumes.kubesan.gitlab.io
spec:
  group: kubesan.gitlab.io
  names:
    categories:
    - kubesan
    - lv
    kind: Volume
    listKind: VolumeList
    plural: volumes
    shortNames:
    - vol
    - vols
    singular: volume
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: '''VG owning the volume'''
      jsonPath: .spec.vgName
      name: VG
      type: string
    - description: '''Path to volume in nodes where it is active'''
      jsonPath: .status.path
      name: Path
      priority: 1
      type: string
    - description: '''Time since volume was available'''
      jsonPath: .status.conditions[?(@.type=="Available")].lastTransitionTime
      name: Available
      type: date
    - description: '''Primary node where volume is currently active'''
      jsonPath: .status.attachedToNodes[0]
      name: Primary Node
      type: string
    - description: '''Size of volume'''
      jsonPath: .status.sizeBytes
      name: Size
      type: integer
    - description: '''Mode of volume (thin or linear)'''
      jsonPath: .spec.mode
      name: Mode
      priority: 2
      type: string
    - description: '''Filesystem type (blank if block)'''
      jsonPath: .spec.type.filesystem.fsType
      name: FSType
      priority: 2
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Volume is the Schema for the volumes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              accessModes:
                description: Should be set from creation and never updated.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
                x-kubernetes-validations:
                - rule: oldSelf==self
              attachToNodes:
                description: May be updated at will.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              contents:
                description: Should be set from creation and never updated.
                properties:
                  cloneSnapshot:
                    properties:
                      sourceSnapshot:
                        type: string
                    required:
                    - sourceSnapshot
                    type: object
                  cloneVolume:
                    properties:
                      sourceVolume:
                        type: string
                    required:
                    - sourceVolume
                    type: object
                  empty:
                    type: object
                type: object
                x-kubernetes-validations:
                - rule: oldSelf==self
              mode:
                description: Should be set from creation and never updated.
                enum:
                - Thin
                - Linear
                type: string
                x-kubernetes-validations:
                - rule: oldSelf==self
              sizeBytes:
                description: Must be positive and a multiple of 512. May be updated
                  at will, but the actual size will only ever increase.
                format: int64
                minimum: 512
                multipleOf: 512
                type: integer
              type:
                description: Should be set from creation and never updated.
                properties:
                  block:
                    type: object
                  filesystem:
                    properties:
                      fsType:
                        type: string
                      mountOptions:
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: atomic
                    required:
                    - fsType
                    type: object
                type: object
                x-kubernetes-validations:
                - rule: oldSelf==self
              vgName:
                description: Should be set from creation and never updated.
                type: string
                x-kubernetes-validations:
                - rule: oldSelf==self
            required:
            - accessModes
            - contents
            - mode
            - sizeBytes
            - type
            - vgName
            type: object
          status:
            properties:
              attachedToNodes:
                description: Reflects the nodes to which the volume is attached.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              conditions:
                description: |-
                  Conditions
                  Available: The LVM volume has been created
                  DataSourceCompleted: Any data source has been copied into the LVM,
                  so that it is now ready to be attached to nodes
                  Abnormal: Indicates the health of a volume
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  The generation of the spec used to produce this status.  Useful
                  as a witness when waiting for status to change.
                format: int64
                type: integer
              path:
                description: The path at which the volume is available on nodes to
                  which it is attached.
                type: string
              sizeBytes:
                description: Reflects the current size of the volume.
                format: int64
                type: integer
                x-kubernetes-validations:
                - rule: oldSelf<=self
            required:
            - observedGeneration
            - sizeBytes
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
