# SPDX-License-Identifier: Apache-2.0

# Code generated by controller-gen. DO NOT EDIT.

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: nbdexports.kubesan.gitlab.io
spec:
  group: kubesan.gitlab.io
  names:
    categories:
    - kubesan
    kind: NBDExport
    listKind: NBDExportList
    plural: nbdexports
    shortNames:
    - nbd
    - nbds
    singular: nbdexport
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: '''LV source of the export'''
      jsonPath: .spec.export
      name: Export
      type: string
    - description: '''Node hosting the export'''
      jsonPath: .spec.host
      name: Host
      type: string
    - description: '''Time since export was available'''
      jsonPath: .status.conditions[?(@.type=="Available")].lastTransitionTime
      name: Available
      type: date
    - description: '''NBD URI for the export'''
      jsonPath: .status.uri
      name: URI
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              clients:
                description: The set of clients connecting to the export.
                items:
                  maxLength: 253
                  minLength: 1
                  pattern: ^[a-z0-9][-.a-z0-9]*$
                  type: string
                type: array
                x-kubernetes-list-type: set
              export:
                description: |-
                  The short name of the LV (Volume or Snapshot) to export, write-once
                  at creation.
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9][-a-z0-9]*$
                type: string
                x-kubernetes-validations:
                - rule: oldSelf==self
              host:
                description: The node hosting the export. Write-once at creation.
                maxLength: 253
                minLength: 1
                pattern: ^[a-z0-9][-.a-z0-9]*$
                type: string
                x-kubernetes-validations:
                - rule: oldSelf==self
              path:
                description: |-
                  The "/dev/..." path of the export, write-once at creation, then
                  cleared to trigger server stop.
                maxLength: 128
                pattern: ^(|/dev/[-_+/a-z0-9]+)$
                type: string
                x-kubernetes-validations:
                - rule: oldSelf==self
              sizeBytes:
                description: The size of the export. Write-once at creation.
                format: int64
                minimum: 512
                multipleOf: 512
                type: integer
                x-kubernetes-validations:
                - rule: oldSelf==self
            required:
            - export
            - host
            - sizeBytes
            type: object
            x-kubernetes-validations:
            - rule: has(self.path)?has(oldSelf.path):true
          status:
            properties:
              conditions:
                description: |-
                  Conditions
                  Available: The export is currently accessible
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  The generation of the spec used to produce this status.  Useful
                  as a witness when waiting for status to change.
                format: int64
                type: integer
                x-kubernetes-validations:
                - rule: self>=oldSelf
              uri:
                description: |-
                  NBD URI for connecting to the NBD export, using IP address.
                  write-once when Conditions["Available"] is first set
                maxLength: 128
                pattern: ^nbd://[0-9a-f:.]+/[a-z][-a-z0-9]*$
                type: string
                x-kubernetes-validations:
                - rule: oldSelf==self
            required:
            - observedGeneration
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
